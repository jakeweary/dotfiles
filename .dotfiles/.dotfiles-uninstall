#!/usr/bin/env bash

backup() {
  git --git-dir=$HOME/.dotfiles.backup --work-tree=$HOME "$@"
}

dotfiles() {
  git --git-dir=$HOME/.dotfiles.git --work-tree=$HOME "$@"
}

pushd ~
  rm -rf $(dotfiles ls-tree -r --name-only HEAD)
  backup reset --hard
  rm -rf .dotfiles.*
popd
