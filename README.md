```sh
# clone over ssh
curl -sSL https://raw.github.com/thepheer/dotfiles/master/.bin/.dotfiles-install | bash -s ssh

# clone over https
curl -sSL https://raw.github.com/thepheer/dotfiles/master/.bin/.dotfiles-install | bash
```
